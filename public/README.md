# Public Assets

This folder contains static assets served by Next.js.

## Favicon

A `favicon.ico` file should be placed here. The file will be automatically served at `/favicon.ico`.

To add a favicon:
1. Create or download a favicon.ico file
2. Place it in this `public/` folder
3. Next.js will automatically serve it
